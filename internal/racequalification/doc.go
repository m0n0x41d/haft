// Package racequalification builds and validates a deterministic, lossless
// partition of Go race-test work. It owns package/test assignment and aggregate
// result policy; command execution and workflow adapters belong outside this
// package.
package racequalification
