// Package typedmemoryevaluation owns immutable, exact-RuleRef registries for
// pure typed-memory evaluator mechanisms.
//
// Registry presence is only executable-mechanism availability. It does not
// admit a Kind, select a TypeEnv, grant authority, validate a semantic change,
// or write project memory. Later composition may compare a registration's
// explicit mechanism identity with a separately sealed runtime basis.
// Production registration construction is sealed inside this package; public
// catalog factories expose only specifically vetted evaluator mechanisms.
package typedmemoryevaluation
