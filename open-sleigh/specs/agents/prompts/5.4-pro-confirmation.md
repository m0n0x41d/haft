# Open-Sleigh — Confirmation Pass Review (v0.6)

You are performing a **focused confirmation-pass review** of the
Open-Sleigh specification set. This is NOT a full first-pass review —
that prompt is `5.4-pro-review.md` and was already run on v0.4.

## Context

A prior run of the full-review prompt on **v0.4** returned
`NEEDS REVISION` with 5 CRITICAL findings and several HIGH findings.
Specs have been revised to **v0.5** (closing the CRITICALs) and then
to **v0.6** (closing operational gaps found by cross-referencing
OpenAI Symphony's SPEC and Elixir implementation at
`.context/symphony/`).

Your job is narrow:

1. **Verify each v0.4 CRITICAL is closed.** Per-finding verdict.
2. **Check for regressions.** Did the v0.5 / v0.6 revisions introduce
   new issues that weren't present in v0.4?
3. **Check v0.6 Symphony-integration soundness.** Did we adopt
   Symphony's operational concepts without re-inheriting its
   anti-patterns (governance sprawl, CLAUDE.md-pathology)?

Focus on levels 1, 2, and 4 of the full-review prompt (target-system
correctness, architecture compliance, Thai-disaster guardrails).
Skip levels 3 (semiotic), 5 (scope), 6 (open questions) unless you
observe a load-bearing issue that requires going there.

## Context-delivery

The attached content should include (in this order):

1. `SPEC.md` — post-v0.7 umbrella index (~300 lines). Narrative +
   pointers only; do NOT expect normative content here.
2. `specs/` — v0.3 decomposition spec set, all normative content
3. `.context/semiotics_slideument.md` — FPF semiotic engineering
4. `.context/hustles/thai/audit-general-analysis.md` — the negative
   exemplar (Thai disaster) that shaped the Thai-disaster guardrails
5. `.context/symphony/SPEC.md` — Symphony's normative spec, the source
   of v0.6 operational-gap additions

For efficiency: skip `.context/FPF-Spec.md` (5 MB, too large for
review context; assume canonical FPF knowledge or ask for specific
patterns); skip `.context/development_for_the_developed.md` unless
you need to verify a phase-name or Slide-12 reference.

## Thinking framework — minimal

- **Object ≠ Description ≠ Carrier.** The specs are Description.
- **Design-time ≠ Run-time.** A constructor is design-time; a passing
  test is run-time. This is a pre-code review so only mechanism-
  soundness is verifiable.
- **Transformer Mandate.** External agent decides; system doesn't
  self-improve; human is the principal.
- **Four-label enforcement taxonomy** (v0.5 introduction): every
  illegal-state row names exactly one of: `type-level` /
  `constructor-level` / `runtime-guard` / `CI-or-module-graph-check`.
  Check that labels match the mechanism described.

---

## Section A — Verify v0.4 CRITICAL closures

Per-finding verdict: **CLOSED / PARTIAL / NOT CLOSED**. For PARTIAL
or NOT CLOSED, name the specific gap.

### CRITICAL-1 — Framing ownership contradiction

**v0.4 finding (historical):** `SYSTEM_CONTEXT.md` said framing is upstream; `SPEC.md §5` (then the canonical authoring site) said Frame phase agent calls `haft_problem(frame)`. Two different target systems. Post-v0.7, the canonical Frame description lives in `specs/target-system/SCOPE_FREEZE.md` + `PHASE_ONTOLOGY.md` + `GATES.md` and the `sleigh.md.example` toolset in `SLEIGH_CONFIG.md` — SPEC.md §5 is a pointer abstract only.

**v0.5/v0.6 response** (post-v0.7 canonical locations noted in square brackets):
- SPEC §5 Frame bullet rewritten: agent **verifies** upstream `ProblemCardRef`; no `haft_problem` in Frame toolset. [SPEC.md §5 now narrative-only; canonical in `SCOPE_FREEZE.md §MVP-1` + `GATES.md §6`.]
- SPEC §5 diagram `[done | reopen]` → `[pass | reopen]`.
- SPEC §6a: new `problem_card_ref_present` structural gate at Frame entry. [`GATES.md §1` + `§6`.]
- SPEC §8 yaml: `phases.frame.tools = [haft_query, read, grep]` (no `haft_problem`). [`SLEIGH_CONFIG.md §1`.]
- `SYSTEM_CONTEXT.md §1`: hard invariant, not advisory.
- `SCOPE_FREEZE.md`: new cut item "ProblemCard authoring by any Open-Sleigh phase".
- `ILLEGAL_STATES.md` UP1 hardened from ⚠️ to ❌.
- `AuthoringRole` sum: `:framer` → `:frame_verifier` renamed across specs.
- `ILLEGAL_STATES.md` CL3 added: Frame-phase agent invoking `haft_problem(frame)` blocked at phase-scoped-tool gate.

**Verdict to provide:** Is the verifier-only commitment now consistent across all specs, OR does residual authoring language survive somewhere?

### CRITICAL-2 — PR5 Evidence self-reference impossible at constructor signature

**v0.4 finding:** `Evidence.new(kind, ref, hash, cl)` has no access to authoring artifact's `self_id`; can't reject `ref == self_id`.

**v0.5 response:**
- `FUNCTIONAL_ARCHITECTURE.md` L1: `Evidence.new/5` no longer claims self-reference check. Check moved to `PhaseOutcome.new/2` where `self_id` is a required provenance field.
- `ILLEGAL_STATES.md` PR5 label: "constructor-level at `PhaseOutcome.new/2`".
- `TARGET_SYSTEM_MODEL.md` Evidence key-rules: explicitly documents "not at Evidence.new".

**Verdict:** Is the check now located where `self_id` is actually available? Is the prior overclaim fully removed (no residual "Evidence.new rejects" language)?

### CRITICAL-3 — `Phase.t()` open while illegal-state claims assume closed

**v0.4 finding:** `Phase.t()` was `:frame | :execute | :measure | {:m2, atom()}` (open atom space), but ILLEGAL_STATES TR5 claimed "arbitrary dynamic phases rejected by closed alphabet".

**v0.5 response:**
- Q-OS-2 closed: `Phase.t()` is a closed sum with all MVP-1 and MVP-2 atoms pre-declared.
- `FUNCTIONAL_ARCHITECTURE.md` L1 Concepts: full alphabet listed.
- `TR5` relabelled to **type-level** enforcement (was constructor-level-discipline).
- `Workflow.mvp1/0` and `Workflow.mvp2/0` route only through their respective subsets of the closed alphabet.

**Verdict:** Is the sum definitively closed? Is TR5's mechanism now actually what the label claims?

### CRITICAL-4 — PhaseOutcome / HumanGateApproval inconsistency

**v0.4 finding:** Q-OS-3 said `PhaseOutcome.new_external/3` when `external_publication: true`, but `TARGET_SYSTEM_MODEL.md` PhaseOutcome attributes had no `external_publication` field.

**v0.5 response:**
- Q-OS-3 closed: single `PhaseOutcome.new/2` constructor.
- Gate-config consistency: if `PhaseConfig.gates.human` declares `commission_approved`, `gate_results` must contain `{:human, HumanGateApproval.t()}`.
- No `external_publication` field on `PhaseOutcome`.
- `TARGET_SYSTEM_MODEL.md` HumanGateApproval section rewritten.
- `ILLEGAL_STATES.md` PR10 rewritten.

**Verdict:** Does the entity model now match the constructor story? Is "external publication" concept expressed consistently (as "phase_config declares a human gate") everywhere?

### CRITICAL-5 — Pre-code gate not passed by spec's own standards

**v0.4 finding:** `OPEN_QUESTIONS.md` had Q-OS-2/3/4 open while the spec proceeded as if they were closed.

**v0.5 response:**
- Q-OS-2, 3, 4 all closed with explicit resolution rationale in `OPEN_QUESTIONS.md`.
- `FUNCTIONAL_ARCHITECTURE.md` "Open architectural questions" section now says "none — all resolved in v0.5" with references.

**Verdict:** Are Q-OS-2/3/4 truly closed, with closures propagated everywhere? Are any still-open questions genuinely non-blocking for `mix new`?

---

## Section B — Regression check from v0.5 / v0.6 revisions

For each v0.5 / v0.6 delta below, verify no NEW issue was introduced:

### v0.5 changes

1. Four-label taxonomy adopted in `ILLEGAL_STATES.md`. Spot-check 5
   random rows — does the label match the described mechanism?
2. Thai-disaster guardrail ratings rewritten honestly (some
   downgraded from "Strong" to "Moderate"). Verify the residual-bypass
   notes are truthful, not hand-wavy.
3. Workspace canonical-path algorithm (CL5–CL12) explicit. Verify the
   algorithm in `FUNCTIONAL_ARCHITECTURE.md L4` matches the enforcement
   labels in `ILLEGAL_STATES.md`.
4. Credo rules enumerated in `STACK_DECISION.md`. Do any of them
   overclaim? (E.g., does "proxy-module rule" actually prevent the
   pattern described, or is it a best-effort static analysis?)

### v0.6 changes (Symphony-integration)

5. **Continuation-turn model** (SPEC §5.1) — within one Execute session,
   N turns on the same thread. Does this conflict with any earlier
   claim that "phase = one atomic agent run"? Verify CT1–CT6 illegal
   states + `AGENT_PROTOCOL.md §3`.
6. **Run-attempt sub-states** (SPEC §5.2). 11 sub-states with distinct
   retry classes. Does `RunAttemptSubState` properly live on
   `Session.t()` (axis 5 of `PHASE_ONTOLOGY.md`) and NOT leak into the
   persisted `PhaseOutcome`?
7. **Workspace hooks** (canonical: `WORKSPACE.md §2`). Hook scripts
   are trusted config but their writes must respect `PathGuard`.
   Verify WH1–WH5 enforcement labels. WH1 is honest about its
   limitation (hook is trusted config, hard to fully prevent). Is
   this acknowledged vs overclaimed?
8. **Stall detection** (canonical: `AGENT_PROTOCOL.md §8`). Orthogonal
   to turn timeout. Verify no double-firing, no missing compensating
   note.
9. **Token accounting** (canonical: `HAFT_CONTRACT.md §4` +
   `AGENT_PROTOCOL.md §4`, illegal-states TA1–TA3). Observations-only,
   never Haft. Verify TA1 enforcement chain (OB5 module graph + Credo
   rule). Is there a proxy route for token counts to enter Haft?
10. **Optional HTTP API** (canonical: `HTTP_API.md`). Observability-
    only, never mutates state, never reads Haft. Verify the
    "`Orchestrator` correctness does NOT depend on HTTP server" claim
    — is there any code path where HTTP is load-bearing?
11. **Reference algorithms** (canonical:
    `../enabling-system/REFERENCE_ALGORITHMS.md`). Non-normative
    skeletons. Do they conflict with any normative section?
12. **`AGENT_PROTOCOL.md`** new file. Verify three explicit FPF
    divergences from Symphony:
    - phase-scoped tool dispatch (not session-scoped)
    - no agent-owned tracker mutation (orchestrator-routed only)
    - continuation turns within one phase (Symphony: within one issue)

---

## Section C — Thai-disaster anti-pattern check (v0.6 regression)

Symphony's `WORKFLOW.md` is a 326-line procedural prompt — the same
pattern as Thai's CLAUDE.md, just smaller. Cross-check:

1. Does our `sleigh.md` example in `SLEIGH_CONFIG.md §1` now encourage
   WORKFLOW.md-style procedural bloat, or does the 300-line budget
   still prevent it? Read the yaml example; does it look terse or
   does it balloon with hooks / prompts?
2. Does the continuation-turn model encourage "agent runs forever on
   one ticket"? Verify `max_turns` cap + tracker-wins reconciliation
   are the brakes.
3. Does the HTTP API surface ever expose Haft artifacts? Verify
   explicit negation in §12.
4. Does the token-accounting pipeline ever pass through Haft? Verify
   TA1 + OB5 together.
5. Do the new workspace hooks encourage non-trivial logic outside the
   agent? Bootstrap (`git clone`, `mix deps.get`) is legitimate. But
   "compile docs" or "run cleanup scripts" would be a slippery slope
   toward governance-in-hooks.

---

## Output format

### 1. Executive verdict

**`CLOSURE CONFIRMED`** / **`CLOSURES INCOMPLETE`** /
**`REGRESSIONS INTRODUCED`** / **`NEEDS ANOTHER PASS`**.

1–2 sentences summary.

### 2. Section A — Per-CRITICAL verdicts

| # | Finding | Verdict | Gap (if partial/not-closed) |
|---|---|---|---|
| C1 | Framing ownership | CLOSED / PARTIAL / NOT CLOSED | |
| C2 | PR5 Evidence self-ref | | |
| C3 | Phase.t() closure | | |
| C4 | PhaseOutcome / approval | | |
| C5 | Pre-code gate | | |

### 3. Section B — Regression findings

Table of any new issue introduced by v0.5 or v0.6 deltas.

| # | Origin (v0.5 or v0.6) | Finding | Severity | Proposed fix |
|---|---|---|---|---|

### 4. Section C — Thai-disaster regression

Table:

| Check | Pass / Concern | Note |
|---|---|---|

### 5. `AGENT_PROTOCOL.md` soundness

3-5 sentence verdict: is the protocol contract complete enough for an
implementer to build a conforming `Codex` adapter? Is it correct
against Symphony's actual Elixir code in `.context/symphony/elixir/`?
Any gap that will bite at implementation?

### 6. Readiness verdict

**`READY TO SCAFFOLD` / `READY WITH MINOR CAVEATS` / `NEEDS ANOTHER REVISION`**

If ready: what's the next step? (Expected: `mix new open_sleigh --sup`
+ first failing canary integration test.)

If not: what's the minimal revision set?

---

## Meta-note

Keep it tight. This is a confirmation pass, not a first-pass audit.
The full review template (`5.4-pro-review.md`) exists for when larger
revisions happen. If you find yourself wanting to give findings
outside Section A / B / C, that's a signal we need another full pass
— flag it and stop, rather than expanding this confirmation.
